function [out] = zeroOut(in)
    % [out] = zeroOut(in)
    % Returns a matrix where true is replaced by 1
    % and false is replaced by NaN
    %
    % Jason Moore
    % Last Edited 12/4/18
    
    out = (1*in)./(1*in);


end